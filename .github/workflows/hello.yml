name: test
on:
   push:
     branches:
       - master
jobs:
   build:
     runs-on: ubuntu-latest

     steps:
       - name: my first step
         uses: actions/checkout@v2

       - name: Say Hello to the World
         run: echo "Hello, World!"